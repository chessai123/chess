import chess
import board
import rules
import math

class Search():
    
    def minmax(curdepth, board,  )
        if curdepth == targetdepth:
            return eval[value]
        
        if (maxplay):
            return max
        else:
            return min
