import chess
import math

class Search():
    
    def minmax(curdepth, ,  )
        if curdepth == targetdepth:
            return eval[value]
        
        if (maxplay):
            return max
        else:
            return min
